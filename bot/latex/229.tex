If $\phi(x)$~is divisible by~$(x - \alpha)^{2}$, then $\phi'(x)$~is divisible by~$x - \alpha$: and
generally, if $\phi(x)$~is divisible by~$(x - \alpha)^{m}$, then $\phi'(x)$~is divisible by~$(x - \alpha)^{m-1}$.

